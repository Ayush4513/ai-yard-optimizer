# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.0.0,<3.0.0
python-dotenv==1.0.0

# Database
neo4j==5.14.1
chromadb==0.4.18  # Client library - server runs in Docker

# LangChain
langchain==0.1.0
langchain-community==0.0.10
langchain-anthropic==0.1.0
langchain-core==0.1.10

# Embeddings
# Note: sentence-transformers 2.2.2 is incompatible with newer huggingface_hub
# Using 2.3.0+ which is compatible with current huggingface_hub versions
sentence-transformers>=2.3.0,<3.0.0
huggingface_hub>=0.20.0,<1.0.0

# Anthropic (Claude) - LLM for RAG
anthropic==0.18.1

# Utilities
python-multipart==0.0.6
httpx==0.25.2
