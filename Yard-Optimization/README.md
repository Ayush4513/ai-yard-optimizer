
  # Yard Optimization (Trying to merge) (Copy)

  This is a code bundle for Yard Optimization (Trying to merge) (Copy). The original project is available at https://www.figma.com/design/xlUeXBnXJvM3VTiZ2IJv2j/Yard-Optimization--Trying-to-merge---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  